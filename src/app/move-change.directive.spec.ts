import { MoveChangeDirective } from './move-change.directive';

describe('MoveChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveChangeDirective();
    expect(directive).toBeTruthy();
  });
});
