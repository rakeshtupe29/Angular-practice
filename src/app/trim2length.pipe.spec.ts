import { Trim2lengthPipe } from './trim2length.pipe';

describe('Trim2lengthPipe', () => {
  it('create an instance', () => {
    const pipe = new Trim2lengthPipe();
    expect(pipe).toBeTruthy();
  });
});
